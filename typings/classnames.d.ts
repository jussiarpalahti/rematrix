
// TODO: classnames import does not work without require, which doesn't work with TS for some reason

declare module "classnames" {
    export module classnames {
        export var classNames;
    }
}
